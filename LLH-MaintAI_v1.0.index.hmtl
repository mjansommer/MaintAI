<!DOCTYPE html>
<html lang="de">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0, viewport-fit=cover">
<title>MaintAI — Predictive Maintenance</title>
<style>
  @import url('https://fonts.googleapis.com/css2?family=DM+Sans:ital,opsz,wght@0,9..40,300;0,9..40,400;0,9..40,500;0,9..40,600;0,9..40,700;1,9..40,400&family=DM+Mono:wght@400;500&display=swap');

  :root {
    --bg: #0A0A0A;
    --bg2: #111111;
    --bg3: #161616;
    --glass: rgba(255,255,255,0.04);
    --glass-border: rgba(255,255,255,0.08);
    --glass-hover: rgba(255,255,255,0.07);
    --accent: #0A84FF;
    --accent2: #30D158;
    --accent3: #FF9F0A;
    --accent4: #FF453A;
    --accent5: #BF5AF2;
    --text: #F5F5F7;
    --text2: #86868B;
    --text3: #48484A;
    --radius: 24px;
    --radius-sm: 14px;
    --radius-xs: 10px;
    --shadow: 0 8px 40px rgba(0,0,0,0.6);
    --shadow-sm: 0 2px 16px rgba(0,0,0,0.4);
    --nav-h: 82px;
    --font: 'DM Sans', -apple-system, BlinkMacSystemFont, sans-serif;
    --mono: 'DM Mono', monospace;
  }

  *, *::before, *::after { box-sizing: border-box; margin: 0; padding: 0; -webkit-tap-highlight-color: transparent; }

  html { font-size: 16px; }

  body {
    font-family: var(--font);
    background: var(--bg);
    color: var(--text);
    min-height: 100vh;
    min-height: 100dvh;
    overflow-x: hidden;
    -webkit-font-smoothing: antialiased;
  }

  /* ===== SCROLLBAR ===== */
  ::-webkit-scrollbar { width: 4px; }
  ::-webkit-scrollbar-track { background: transparent; }
  ::-webkit-scrollbar-thumb { background: var(--text3); border-radius: 4px; }

  /* ===== APP SHELL ===== */
  #app { position: relative; width: 100%; min-height: 100dvh; padding-bottom: var(--nav-h); }

  /* ===== PAGES ===== */
  .page {
    display: none;
    padding: 0 0 24px;
    animation: pageFadeIn 0.28s cubic-bezier(0.4,0,0.2,1);
  }
  .page.active { display: block; }

  @keyframes pageFadeIn {
    from { opacity: 0; transform: translateY(10px); }
    to   { opacity: 1; transform: translateY(0); }
  }

  /* ===== PAGE HEADER ===== */
  .page-header {
    position: sticky;
    top: 0;
    z-index: 50;
    padding: 56px 20px 16px;
    background: linear-gradient(180deg, var(--bg) 70%, transparent);
    backdrop-filter: blur(20px);
    -webkit-backdrop-filter: blur(20px);
  }
  .page-header h1 {
    font-size: 28px;
    font-weight: 700;
    letter-spacing: -0.5px;
    line-height: 1.1;
  }
  .page-header p { font-size: 14px; color: var(--text2); margin-top: 4px; }
  .header-row { display: flex; align-items: center; justify-content: space-between; }

  /* ===== BOTTOM NAV ===== */
  .bottom-nav {
    position: fixed;
    bottom: 0;
    left: 0; right: 0;
    z-index: 100;
    padding: 0 8px;
    padding-bottom: env(safe-area-inset-bottom, 8px);
    background: rgba(10,10,10,0.85);
    backdrop-filter: blur(30px) saturate(180%);
    -webkit-backdrop-filter: blur(30px) saturate(180%);
    border-top: 1px solid var(--glass-border);
    display: flex;
    align-items: center;
    justify-content: space-around;
    height: var(--nav-h);
  }
  .nav-item {
    display: flex;
    flex-direction: column;
    align-items: center;
    gap: 4px;
    cursor: pointer;
    padding: 8px 12px;
    border-radius: var(--radius-xs);
    transition: all 0.2s;
    flex: 1;
    max-width: 72px;
    position: relative;
  }
  .nav-item .nav-icon {
    width: 24px; height: 24px;
    display: flex; align-items: center; justify-content: center;
    position: relative;
  }
  .nav-item svg { width: 22px; height: 22px; transition: all 0.2s; }
  .nav-item span {
    font-size: 10px;
    font-weight: 500;
    color: var(--text3);
    transition: color 0.2s;
    letter-spacing: 0.2px;
  }
  .nav-item.active svg { stroke: var(--accent); }
  .nav-item.active span { color: var(--accent); }
  .nav-item:not(.active) svg { stroke: var(--text3); }
  .nav-badge {
    position: absolute;
    top: -4px; right: -4px;
    background: var(--accent4);
    color: white;
    font-size: 9px;
    font-weight: 700;
    width: 16px; height: 16px;
    border-radius: 50%;
    display: flex; align-items: center; justify-content: center;
    border: 2px solid var(--bg);
  }

  /* ===== CARDS ===== */
  .card {
    background: var(--glass);
    border: 1px solid var(--glass-border);
    border-radius: var(--radius);
    padding: 20px;
    backdrop-filter: blur(10px);
    transition: all 0.2s cubic-bezier(0.4,0,0.2,1);
  }
  .card:active { transform: scale(0.98); }
  .card-sm { border-radius: var(--radius-sm); padding: 16px; }
  .card-xs { border-radius: var(--radius-xs); padding: 12px; }

  /* ===== KPI GRID ===== */
  .kpi-grid {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: 12px;
    padding: 0 16px;
    margin-bottom: 20px;
  }
  .kpi-card {
    background: var(--glass);
    border: 1px solid var(--glass-border);
    border-radius: var(--radius-sm);
    padding: 18px 16px;
    position: relative;
    overflow: hidden;
  }
  .kpi-card::before {
    content: '';
    position: absolute;
    top: 0; left: 0; right: 0;
    height: 2px;
    border-radius: 2px 2px 0 0;
  }
  .kpi-card.blue::before { background: var(--accent); }
  .kpi-card.green::before { background: var(--accent2); }
  .kpi-card.orange::before { background: var(--accent3); }
  .kpi-card.red::before { background: var(--accent4); }
  .kpi-icon {
    width: 36px; height: 36px;
    border-radius: 10px;
    display: flex; align-items: center; justify-content: center;
    margin-bottom: 12px;
  }
  .kpi-icon.blue { background: rgba(10,132,255,0.15); }
  .kpi-icon.green { background: rgba(48,209,88,0.15); }
  .kpi-icon.orange { background: rgba(255,159,10,0.15); }
  .kpi-icon.red { background: rgba(255,69,58,0.15); }
  .kpi-value {
    font-size: 30px;
    font-weight: 700;
    letter-spacing: -1px;
    line-height: 1;
    margin-bottom: 4px;
  }
  .kpi-label { font-size: 12px; color: var(--text2); font-weight: 500; }
  .kpi-trend { font-size: 11px; margin-top: 6px; }
  .kpi-trend.up { color: var(--accent2); }
  .kpi-trend.down { color: var(--accent4); }

  /* ===== SECTION ===== */
  .section { padding: 0 16px; margin-bottom: 24px; }
  .section-title {
    font-size: 13px;
    font-weight: 600;
    color: var(--text2);
    text-transform: uppercase;
    letter-spacing: 0.8px;
    margin-bottom: 12px;
  }

  /* ===== STATUS BADGE ===== */
  .badge {
    display: inline-flex; align-items: center; gap: 5px;
    padding: 4px 10px;
    border-radius: 20px;
    font-size: 11px;
    font-weight: 600;
    letter-spacing: 0.2px;
  }
  .badge::before { content: ''; width: 6px; height: 6px; border-radius: 50%; }
  .badge.green { background: rgba(48,209,88,0.15); color: var(--accent2); }
  .badge.green::before { background: var(--accent2); box-shadow: 0 0 6px var(--accent2); }
  .badge.orange { background: rgba(255,159,10,0.15); color: var(--accent3); }
  .badge.orange::before { background: var(--accent3); }
  .badge.red { background: rgba(255,69,58,0.15); color: var(--accent4); }
  .badge.red::before { background: var(--accent4); animation: pulse-red 1.5s infinite; }
  .badge.blue { background: rgba(10,132,255,0.15); color: var(--accent); }
  .badge.blue::before { background: var(--accent); }
  .badge.gray { background: rgba(255,255,255,0.08); color: var(--text2); }
  .badge.gray::before { background: var(--text2); }

  @keyframes pulse-red {
    0%,100% { box-shadow: 0 0 0 0 rgba(255,69,58,0.4); }
    50% { box-shadow: 0 0 0 4px rgba(255,69,58,0); }
  }

  /* ===== TICKET CARD ===== */
  .ticket-item {
    background: var(--glass);
    border: 1px solid var(--glass-border);
    border-radius: var(--radius-sm);
    padding: 16px;
    margin-bottom: 10px;
    cursor: pointer;
    transition: all 0.2s;
    position: relative;
    overflow: hidden;
  }
  .ticket-item:active { transform: scale(0.99); background: var(--glass-hover); }
  .ticket-item .ticket-id { font-size: 11px; color: var(--text3); font-family: var(--mono); }
  .ticket-item .ticket-title { font-size: 15px; font-weight: 600; margin: 4px 0; }
  .ticket-item .ticket-meta { display: flex; align-items: center; gap: 8px; flex-wrap: wrap; }
  .ticket-item .ticket-machine { font-size: 12px; color: var(--text2); }
  .ticket-prio {
    position: absolute;
    top: 0; left: 0;
    width: 3px;
    height: 100%;
    border-radius: 3px 0 0 3px;
  }
  .ticket-prio.critical { background: var(--accent4); }
  .ticket-prio.high { background: var(--accent3); }
  .ticket-prio.medium { background: var(--accent); }
  .ticket-prio.low { background: var(--accent2); }

  /* ===== INPUT FIELDS ===== */
  .input-group { margin-bottom: 16px; }
  .input-label { font-size: 13px; font-weight: 600; color: var(--text2); margin-bottom: 8px; display: block; }
  .input-field {
    width: 100%;
    background: var(--glass);
    border: 1px solid var(--glass-border);
    border-radius: var(--radius-sm);
    padding: 14px 16px;
    color: var(--text);
    font-family: var(--font);
    font-size: 15px;
    outline: none;
    transition: all 0.2s;
    -webkit-appearance: none;
    appearance: none;
  }
  .input-field:focus { border-color: var(--accent); background: rgba(10,132,255,0.05); }
  .input-field::placeholder { color: var(--text3); }
  textarea.input-field { resize: none; min-height: 100px; }
  select.input-field { cursor: pointer; }
  select.input-field option { background: #1c1c1e; color: var(--text); }

  /* ===== BUTTON ===== */
  .btn {
    display: inline-flex; align-items: center; justify-content: center; gap: 8px;
    padding: 14px 24px;
    border-radius: var(--radius-sm);
    font-family: var(--font);
    font-size: 15px;
    font-weight: 600;
    cursor: pointer;
    border: none;
    transition: all 0.2s cubic-bezier(0.4,0,0.2,1);
    -webkit-appearance: none;
    width: 100%;
    letter-spacing: -0.2px;
  }
  .btn-primary { background: var(--accent); color: white; }
  .btn-primary:active { background: #0060cc; transform: scale(0.98); }
  .btn-secondary { background: var(--glass); border: 1px solid var(--glass-border); color: var(--text); }
  .btn-secondary:active { background: var(--glass-hover); transform: scale(0.98); }
  .btn-danger { background: rgba(255,69,58,0.15); border: 1px solid rgba(255,69,58,0.2); color: var(--accent4); }
  .btn-sm { padding: 10px 18px; font-size: 13px; border-radius: var(--radius-xs); width: auto; }
  .btn-icon {
    background: var(--glass);
    border: 1px solid var(--glass-border);
    border-radius: var(--radius-xs);
    padding: 10px;
    cursor: pointer;
    display: flex; align-items: center; justify-content: center;
    transition: all 0.2s;
  }
  .btn-icon:active { transform: scale(0.95); }

  /* ===== CHART ===== */
  .chart-container { position: relative; height: 140px; padding: 0 4px; }
  .chart-bars { display: flex; align-items: flex-end; gap: 6px; height: 100%; }
  .chart-bar-wrap { flex: 1; display: flex; flex-direction: column; align-items: center; gap: 4px; height: 100%; justify-content: flex-end; }
  .chart-bar {
    width: 100%;
    border-radius: 6px 6px 0 0;
    transition: height 0.8s cubic-bezier(0.4,0,0.2,1);
    min-height: 4px;
  }
  .chart-bar.accent { background: var(--accent); }
  .chart-bar.red { background: var(--accent4); }
  .chart-label { font-size: 10px; color: var(--text3); font-family: var(--mono); }

  /* ===== MACHINE CARD ===== */
  .machine-card {
    background: var(--glass);
    border: 1px solid var(--glass-border);
    border-radius: var(--radius-sm);
    padding: 16px;
    margin-bottom: 10px;
    display: flex; align-items: center; gap: 14px;
    cursor: pointer;
    transition: all 0.2s;
  }
  .machine-card:active { transform: scale(0.99); }
  .machine-icon {
    width: 44px; height: 44px;
    border-radius: 12px;
    background: var(--glass);
    border: 1px solid var(--glass-border);
    display: flex; align-items: center; justify-content: center;
    flex-shrink: 0;
    font-size: 20px;
  }
  .machine-info { flex: 1; min-width: 0; }
  .machine-name { font-size: 15px; font-weight: 600; }
  .machine-loc { font-size: 12px; color: var(--text2); margin-top: 2px; }
  .machine-last { font-size: 11px; color: var(--text3); margin-top: 4px; font-family: var(--mono); }

  /* ===== KB ===== */
  .kb-item {
    background: var(--glass);
    border: 1px solid var(--glass-border);
    border-radius: var(--radius-sm);
    padding: 16px;
    margin-bottom: 10px;
    cursor: pointer;
    transition: all 0.2s;
  }
  .kb-item:active { background: var(--glass-hover); }
  .kb-tag {
    display: inline-block;
    background: rgba(10,132,255,0.15);
    color: var(--accent);
    font-size: 10px; font-weight: 700;
    padding: 3px 8px; border-radius: 6px;
    margin-bottom: 8px; letter-spacing: 0.5px;
  }
  .kb-title { font-size: 15px; font-weight: 600; margin-bottom: 4px; }
  .kb-excerpt { font-size: 13px; color: var(--text2); line-height: 1.4; }

  /* ===== QR ===== */
  .qr-scanner {
    background: var(--glass);
    border: 1px solid var(--glass-border);
    border-radius: var(--radius);
    margin: 0 16px 20px;
    aspect-ratio: 1;
    display: flex;
    flex-direction: column;
    align-items: center;
    justify-content: center;
    gap: 16px;
    position: relative;
    overflow: hidden;
  }
  .qr-frame {
    width: 60%;
    aspect-ratio: 1;
    position: relative;
  }
  .qr-corner {
    position: absolute;
    width: 24px; height: 24px;
    border-color: var(--accent);
    border-style: solid;
  }
  .qr-corner.tl { top:0; left:0; border-width: 3px 0 0 3px; border-radius: 4px 0 0 0; }
  .qr-corner.tr { top:0; right:0; border-width: 3px 3px 0 0; border-radius: 0 4px 0 0; }
  .qr-corner.bl { bottom:0; left:0; border-width: 0 0 3px 3px; border-radius: 0 0 0 4px; }
  .qr-corner.br { bottom:0; right:0; border-width: 0 3px 3px 0; border-radius: 0 0 4px 0; }
  .qr-scan-line {
    position: absolute;
    width: 100%; height: 2px;
    background: linear-gradient(90deg, transparent, var(--accent), transparent);
    animation: scanLine 2s ease-in-out infinite;
    border-radius: 1px;
  }
  @keyframes scanLine {
    0%,100% { top: 0; opacity: 1; }
    50% { top: 100%; opacity: 0.6; }
  }
  .qr-hint { font-size: 13px; color: var(--text2); text-align: center; }
  .qr-machines { display: grid; grid-template-columns: 1fr 1fr; gap: 10px; padding: 0 16px; }
  .qr-machine-btn {
    background: var(--glass);
    border: 1px solid var(--glass-border);
    border-radius: var(--radius-sm);
    padding: 16px 12px;
    text-align: center;
    cursor: pointer;
    transition: all 0.2s;
  }
  .qr-machine-btn:active { background: var(--glass-hover); transform: scale(0.97); }
  .qr-machine-btn .icon { font-size: 28px; margin-bottom: 8px; }
  .qr-machine-btn .name { font-size: 13px; font-weight: 600; }
  .qr-machine-btn .id { font-size: 11px; color: var(--text2); font-family: var(--mono); }

  /* ===== MODAL ===== */
  .modal-overlay {
    position: fixed; inset: 0; z-index: 200;
    background: rgba(0,0,0,0.7);
    backdrop-filter: blur(8px);
    -webkit-backdrop-filter: blur(8px);
    display: flex; align-items: flex-end;
    animation: overlayIn 0.2s ease;
  }
  @keyframes overlayIn { from { opacity:0; } to { opacity:1; } }
  .modal {
    background: #1a1a1a;
    border: 1px solid var(--glass-border);
    border-radius: var(--radius) var(--radius) 0 0;
    padding: 20px 20px 40px;
    width: 100%;
    max-height: 90dvh;
    overflow-y: auto;
    animation: modalUp 0.3s cubic-bezier(0.4,0,0.2,1);
  }
  @keyframes modalUp {
    from { transform: translateY(100%); }
    to   { transform: translateY(0); }
  }
  .modal-handle {
    width: 36px; height: 4px;
    background: var(--text3);
    border-radius: 2px;
    margin: 0 auto 20px;
  }
  .modal-title { font-size: 20px; font-weight: 700; margin-bottom: 20px; letter-spacing: -0.3px; }

  /* ===== TOAST ===== */
  #toast-container {
    position: fixed; top: 60px; left: 50%; transform: translateX(-50%);
    z-index: 999; display: flex; flex-direction: column; gap: 8px; align-items: center;
    pointer-events: none;
  }
  .toast {
    background: rgba(28,28,30,0.95);
    border: 1px solid var(--glass-border);
    border-radius: 14px;
    padding: 10px 18px;
    font-size: 13px;
    font-weight: 500;
    color: var(--text);
    backdrop-filter: blur(20px);
    display: flex; align-items: center; gap: 8px;
    white-space: nowrap;
    box-shadow: var(--shadow);
    animation: toastIn 0.3s cubic-bezier(0.4,0,0.2,1);
  }
  .toast.fade-out { animation: toastOut 0.3s ease forwards; }
  @keyframes toastIn { from { opacity:0; transform: translateY(-10px) scale(0.95); } to { opacity:1; transform:none; } }
  @keyframes toastOut { from { opacity:1; } to { opacity:0; transform: scale(0.9); } }

  /* ===== LOADING ===== */
  .skeleton {
    background: linear-gradient(90deg, var(--glass) 25%, rgba(255,255,255,0.06) 50%, var(--glass) 75%);
    background-size: 200% 100%;
    animation: shimmer 1.5s infinite;
    border-radius: var(--radius-xs);
  }
  @keyframes shimmer { 0% { background-position: 200% 0; } 100% { background-position: -200% 0; } }

  /* ===== IMAGE PREVIEW ===== */
  .img-preview {
    width: 100%; aspect-ratio: 16/9;
    object-fit: cover;
    border-radius: var(--radius-sm);
    margin: 0 16px 16px;
    width: calc(100% - 32px);
    border: 1px solid var(--glass-border);
  }

  /* ===== MISC ===== */
  .divider { height: 1px; background: var(--glass-border); margin: 16px 0; }
  .text-accent { color: var(--accent); }
  .text-green { color: var(--accent2); }
  .text-orange { color: var(--accent3); }
  .text-red { color: var(--accent4); }
  .text-muted { color: var(--text2); }
  .fw-700 { font-weight: 700; }
  .flex { display: flex; }
  .flex-between { display: flex; justify-content: space-between; align-items: center; }
  .gap-8 { gap: 8px; }
  .mt-8 { margin-top: 8px; }
  .mb-8 { margin-bottom: 8px; }
  .hidden { display: none !important; }
  .mono { font-family: var(--mono); }

  /* ===== MACHINE DETAIL ===== */
  .hist-item {
    display: flex; gap: 12px; align-items: flex-start;
    padding: 12px 0;
    border-bottom: 1px solid var(--glass-border);
  }
  .hist-dot {
    width: 10px; height: 10px; border-radius: 50%;
    flex-shrink: 0; margin-top: 4px;
  }
  .hist-date { font-size: 11px; color: var(--text3); font-family: var(--mono); margin-top: 2px; }

  /* === Dashboard ticker === */
  .status-strip {
    display: flex; gap: 8px;
    padding: 0 16px 16px;
    overflow-x: auto;
    scrollbar-width: none;
    -webkit-overflow-scrolling: touch;
  }
  .status-strip::-webkit-scrollbar { display: none; }
  .status-chip {
    flex-shrink: 0;
    background: var(--glass);
    border: 1px solid var(--glass-border);
    border-radius: 20px;
    padding: 8px 14px;
    display: flex; align-items: center; gap: 8px;
    white-space: nowrap;
  }
  .status-chip .dot { width: 8px; height: 8px; border-radius: 50%; }
  .status-chip .chip-name { font-size: 12px; font-weight: 600; }
  .status-chip .chip-status { font-size: 11px; color: var(--text2); }

  /* === search bar === */
  .search-bar {
    display: flex; align-items: center; gap: 10px;
    background: var(--glass);
    border: 1px solid var(--glass-border);
    border-radius: var(--radius-sm);
    padding: 12px 16px;
    margin: 0 16px 16px;
  }
  .search-bar input {
    background: none; border: none; outline: none;
    color: var(--text); font-family: var(--font); font-size: 15px; flex: 1;
  }
  .search-bar input::placeholder { color: var(--text3); }

  /* filter tabs */
  .filter-tabs {
    display: flex; gap: 8px;
    padding: 0 16px 16px;
    overflow-x: auto; scrollbar-width: none;
  }
  .filter-tabs::-webkit-scrollbar { display: none; }
  .ftab {
    flex-shrink: 0;
    padding: 8px 16px;
    border-radius: 20px;
    font-size: 13px; font-weight: 600;
    cursor: pointer;
    transition: all 0.2s;
    border: 1px solid var(--glass-border);
    background: var(--glass);
    color: var(--text2);
  }
  .ftab.active { background: var(--accent); border-color: var(--accent); color: white; }

  /* file attach preview */
  .attach-preview {
    display: flex; align-items: center; gap: 10px;
    background: var(--glass);
    border: 1px solid var(--glass-border);
    border-radius: var(--radius-xs);
    padding: 10px 14px;
    margin-top: 8px;
  }
  .attach-thumb {
    width: 40px; height: 40px;
    border-radius: 8px;
    object-fit: cover;
    background: var(--bg3);
    display: flex; align-items: center; justify-content: center;
    font-size: 20px;
    flex-shrink: 0;
  }

  /* logo mark */
  .logo-mark {
    display: flex; align-items: center; gap: 8px;
  }
  .logo-icon {
    width: 32px; height: 32px;
    background: linear-gradient(135deg, var(--accent), #7B2FFF);
    border-radius: 9px;
    display: flex; align-items: center; justify-content: center;
    font-size: 16px;
  }
  .logo-text { font-size: 20px; font-weight: 700; letter-spacing: -0.5px; }
  .logo-text span { color: var(--accent); }

  @media(min-width:500px) {
    .kpi-grid { grid-template-columns: repeat(4,1fr); }
    .risk-grid { grid-template-columns: repeat(4,1fr); }
    .qr-machines { grid-template-columns: repeat(3,1fr); }
  }
</style>
</head>
<body>

<div id="app">

  <!-- ============ TOAST ============ -->
  <div id="toast-container"></div>

  <!-- ============ PAGE: DASHBOARD ============ -->
  <div class="page active" id="page-dashboard">
    <div class="page-header">
      <div class="header-row">
        <div class="logo-mark">
          <div class="logo-icon">⚡</div>
          <div class="logo-text">Maint<span>AI</span></div>
        </div>
        <div style="display:flex;gap:8px">
          <div class="btn-icon" onclick="showToast('🔔 Alle Benachrichtigungen gelesen','green')">
            <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" width="20" height="20"><path d="M18 8A6 6 0 0 0 6 8c0 7-3 9-3 9h18s-3-2-3-9"/><path d="M13.73 21a2 2 0 0 1-3.46 0"/></svg>
          </div>
        </div>
      </div>
      <p style="margin-top:6px;font-size:13px;color:var(--text2)">Guten Morgen! Hier ist dein Überblick.</p>
    </div>

    <!-- Status Strip -->
    <div class="status-strip" id="dashboard-strip"></div>

    <!-- KPI Grid -->
    <div class="kpi-grid" id="dashboard-kpis"></div>

    <!-- Chart -->
    <div class="section">
      <div class="card">
        <div class="flex-between mb-8">
          <div>
            <div style="font-size:15px;font-weight:700">Störungen (7 Tage)</div>
            <div style="font-size:12px;color:var(--text2);margin-top:2px">Gesamt: <span id="total-incidents" style="color:var(--accent);font-weight:700">—</span></div>
          </div>
          <div class="badge blue">Live</div>
        </div>
        <div class="chart-container">
          <div class="chart-bars" id="dashboard-chart"></div>
        </div>
      </div>
    </div>

    <!-- Recent Tickets -->
    <div class="section">
      <div class="section-title">Aktuelle Tickets</div>
      <div id="dashboard-tickets"></div>
    </div>
  </div>

  <!-- ============ PAGE: TICKETS ============ -->
  <div class="page" id="page-tickets">
    <div class="page-header">
      <div class="header-row">
        <div>
          <h1>Tickets</h1>
          <p id="ticket-count-label">Lade...</p>
        </div>
        <button class="btn btn-primary btn-sm" onclick="openTicketModal()">+ Neu</button>
      </div>
    </div>

    <div class="filter-tabs" id="ticket-filters">
      <div class="ftab active" onclick="filterTickets('all',this)">Alle</div>
      <div class="ftab" onclick="filterTickets('open',this)">Offen</div>
      <div class="ftab" onclick="filterTickets('in-progress',this)">In Arbeit</div>
      <div class="ftab" onclick="filterTickets('done',this)">Erledigt</div>
      <div class="ftab" onclick="filterTickets('critical',this)">Kritisch</div>
    </div>

    <div class="section" id="tickets-list"></div>
  </div>

  <!-- ============ PAGE: MASCHINEN ============ -->
  <div class="page" id="page-maschinen">
    <div class="page-header">
      <h1>Maschinen</h1>
      <p>Statusübersicht aller Anlagen</p>
    </div>

    <div class="search-bar">
      <svg viewBox="0 0 24 24" fill="none" stroke="var(--text3)" stroke-width="2" width="18" height="18"><circle cx="11" cy="11" r="8"/><path d="m21 21-4.35-4.35"/></svg>
      <input type="text" placeholder="Maschine suchen..." oninput="searchMachines(this.value)" id="machine-search">
    </div>

    <div class="section" id="machines-list"></div>
  </div>

  <!-- ============ PAGE: QR / KB / EXTRA ============ -->
  <div class="page" id="page-qr">
    <div class="page-header">
      <h1>Mehr</h1>
      <p>QR-Scanner, Wissensdatenbank & mehr</p>
    </div>

    <!-- Quick Action Cards -->
    <div style="display:grid;grid-template-columns:1fr 1fr;gap:12px;padding:0 16px 20px">
      <div class="card" style="cursor:pointer;text-align:center" onclick="showSection('qr-scanner')">
        <div style="font-size:36px;margin-bottom:10px">📱</div>
        <div style="font-size:14px;font-weight:700;margin-bottom:4px">QR-Scanner</div>
        <div style="font-size:12px;color:var(--text2)">Maschinen scannen</div>
      </div>
      <div class="card" style="cursor:pointer;text-align:center" onclick="showSection('kb')">
        <div style="font-size:36px;margin-bottom:10px">📚</div>
        <div style="font-size:14px;font-weight:700;margin-bottom:4px">Wissensdatenbank</div>
        <div style="font-size:12px;color:var(--text2)">Fehler & Lösungen</div>
      </div>
      <div class="card" style="cursor:pointer;text-align:center" onclick="showToast('📊 Bericht wird erstellt...','blue')">
        <div style="font-size:36px;margin-bottom:10px">📊</div>
        <div style="font-size:14px;font-weight:700;margin-bottom:4px">Berichte</div>
        <div style="font-size:12px;color:var(--text2)">Export & Analyse</div>
      </div>
      <div class="card" style="cursor:pointer;text-align:center" onclick="showToast('⚙️ Einstellungen geöffnet','gray')">
        <div style="font-size:36px;margin-bottom:10px">⚙️</div>
        <div style="font-size:14px;font-weight:700;margin-bottom:4px">Einstellungen</div>
        <div style="font-size:12px;color:var(--text2)">System & Profil</div>
      </div>
    </div>

    <!-- QR Section -->
    <div id="qr-scanner" class="hidden">
      <div class="section"><div class="section-title">QR-Scanner</div></div>
      <div class="qr-scanner">
        <div class="qr-frame">
          <div class="qr-corner tl"></div>
          <div class="qr-corner tr"></div>
          <div class="qr-corner bl"></div>
          <div class="qr-corner br"></div>
          <div class="qr-scan-line"></div>
        </div>
        <div class="qr-hint">Richte die Kamera auf den Maschinen-QR-Code<br>oder wähle eine Maschine direkt aus:</div>
      </div>
      <div class="qr-machines" id="qr-machine-btns"></div>
    </div>

    <!-- KB Section -->
    <div id="kb" class="hidden">
      <div class="search-bar" style="margin-top:0">
        <svg viewBox="0 0 24 24" fill="none" stroke="var(--text3)" stroke-width="2" width="18" height="18"><circle cx="11" cy="11" r="8"/><path d="m21 21-4.35-4.35"/></svg>
        <input type="text" placeholder="Fehler, Lösung suchen..." id="kb-search" oninput="searchKB(this.value)">
      </div>
      <div class="filter-tabs">
        <div class="ftab active" onclick="filterKB('all',this)">Alle</div>
        <div class="ftab" onclick="filterKB('motor',this)">Motor</div>
        <div class="ftab" onclick="filterKB('hydraulik',this)">Hydraulik</div>
        <div class="ftab" onclick="filterKB('elektrik',this)">Elektrik</div>
        <div class="ftab" onclick="filterKB('wartung',this)">Wartung</div>
      </div>
      <div class="section" id="kb-list"></div>
    </div>
  </div>

  <!-- ============ BOTTOM NAV ============ -->
  <nav class="bottom-nav">
    <div class="nav-item active" data-page="dashboard" onclick="navigate('dashboard',this)">
      <div class="nav-icon">
        <svg viewBox="0 0 24 24" fill="none" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><rect x="3" y="3" width="7" height="7"/><rect x="14" y="3" width="7" height="7"/><rect x="14" y="14" width="7" height="7"/><rect x="3" y="14" width="7" height="7"/></svg>
      </div>
      <span>Dashboard</span>
    </div>
    <div class="nav-item" data-page="tickets" onclick="navigate('tickets',this)">
      <div class="nav-icon">
        <div class="nav-badge" id="ticket-badge">0</div>
        <svg viewBox="0 0 24 24" fill="none" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M14 2H6a2 2 0 0 0-2 2v16a2 2 0 0 0 2 2h12a2 2 0 0 0 2-2V8z"/><polyline points="14 2 14 8 20 8"/><line x1="16" y1="13" x2="8" y2="13"/><line x1="16" y1="17" x2="8" y2="17"/><polyline points="10 9 9 9 8 9"/></svg>
      </div>
      <span>Tickets</span>
    </div>
    <div class="nav-item" data-page="maschinen" onclick="navigate('maschinen',this)">
      <div class="nav-icon">
        <svg viewBox="0 0 24 24" fill="none" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><rect x="2" y="7" width="20" height="14" rx="2"/><path d="M16 7V5a2 2 0 0 0-2-2h-4a2 2 0 0 0-2 2v2"/><line x1="12" y1="12" x2="12" y2="16"/><line x1="10" y1="14" x2="14" y2="14"/></svg>
      </div>
      <span>Maschinen</span>
    </div>
  </nav>

</div>

<!-- ============ MODALS ============ -->

<!-- Ticket Modal -->
<div class="modal-overlay hidden" id="ticket-modal" onclick="if(event.target===this)closeModal('ticket-modal')">
  <div class="modal">
    <div class="modal-handle"></div>
    <div class="modal-title">Neues Ticket</div>
    <div class="input-group">
      <label class="input-label">Titel</label>
      <input type="text" class="input-field" id="t-title" placeholder="Kurze Fehlerbeschreibung">
    </div>
    <div class="input-group">
      <label class="input-label">Maschine</label>
      <select class="input-field" id="t-machine"></select>
    </div>
    <div class="input-group">
      <label class="input-label">Priorität</label>
      <select class="input-field" id="t-prio">
        <option value="low">🟢 Niedrig</option>
        <option value="medium" selected>🔵 Mittel</option>
        <option value="high">🟠 Hoch</option>
        <option value="critical">🔴 Kritisch</option>
      </select>
    </div>
    <div class="input-group">
      <label class="input-label">Beschreibung</label>
      <textarea class="input-field" id="t-desc" placeholder="Detaillierte Fehlerbeschreibung..."></textarea>
    </div>
    <div class="input-group">
      <label class="input-label">Foto anhängen</label>
      <button class="btn btn-secondary" onclick="document.getElementById('t-file').click()">📎 Datei auswählen</button>
      <input type="file" id="t-file" class="hidden" accept="image/*" onchange="previewAttach(event)">
      <div id="t-attach-preview" class="hidden attach-preview">
        <div class="attach-thumb" id="t-attach-icon">📎</div>
        <div>
          <div style="font-size:13px;font-weight:600" id="t-attach-name">–</div>
          <div style="font-size:11px;color:var(--text2)">Bereit zum Hochladen</div>
        </div>
      </div>
    </div>
    <button class="btn btn-primary" onclick="createTicket()">✓ Ticket erstellen</button>
  </div>
</div>

<!-- Machine Detail Modal -->
<div class="modal-overlay hidden" id="machine-modal" onclick="if(event.target===this)closeModal('machine-modal')">
  <div class="modal" id="machine-modal-content"></div>
</div>

<!-- Ticket Detail Modal -->
<div class="modal-overlay hidden" id="ticket-detail-modal" onclick="if(event.target===this)closeModal('ticket-detail-modal')">
  <div class="modal" id="ticket-detail-content"></div>
</div>

<!-- QR Machine Detail Modal -->
<div class="modal-overlay hidden" id="qr-detail-modal" onclick="if(event.target===this)closeModal('qr-detail-modal')">
  <div class="modal" id="qr-detail-content"></div>
</div>

<!-- KB Detail Modal -->
<div class="modal-overlay hidden" id="kb-detail-modal" onclick="if(event.target===this)closeModal('kb-detail-modal')">
  <div class="modal" id="kb-detail-content"></div>
</div>

<style>
@keyframes spin { from { transform: rotate(0deg); } to { transform: rotate(360deg); } }
</style>

<script>
// ===================== DATA =====================

const MACHINES = [
  { id:'M001', name:'CNC Fräsmaschine Alpha', icon:'🔧', loc:'Halle A - Linie 1', status:'online', lastMaint:'vor 12 Tagen', nextMaint:'in 18 Tagen', runtime:'2.847h', temp:'62°C', vib:'0.3 g', errors:3 },
  { id:'M002', name:'Hydraulikpresse BP-200', icon:'⚙️', loc:'Halle B - Linie 3', status:'warning', lastMaint:'vor 45 Tagen', nextMaint:'in 3 Tagen', runtime:'5.102h', temp:'78°C', vib:'1.2 g', errors:7 },
  { id:'M003', name:'Schweißroboter WR-X', icon:'🤖', loc:'Halle A - Linie 2', status:'online', lastMaint:'vor 5 Tagen', nextMaint:'in 25 Tagen', runtime:'1.204h', temp:'55°C', vib:'0.1 g', errors:1 },
  { id:'M004', name:'Kompressor K-440', icon:'💨', loc:'Technikraum 1', status:'critical', lastMaint:'vor 67 Tagen', nextMaint:'überfällig', runtime:'8.543h', temp:'94°C', vib:'3.1 g', errors:14 },
  { id:'M005', name:'Förderbandanlage FB-12', icon:'📦', loc:'Halle C - Logistik', status:'online', lastMaint:'vor 3 Tagen', nextMaint:'in 27 Tagen', runtime:'3.789h', temp:'42°C', vib:'0.4 g', errors:2 },
  { id:'M006', name:'Drehmaschine DM-500', icon:'🔩', loc:'Halle B - Linie 1', status:'offline', lastMaint:'vor 90 Tagen', nextMaint:'überfällig', runtime:'0h', temp:'–', vib:'–', errors:5 },
];

const MACHINE_HISTORY = {
  'M001': [
    { date:'12.05.2025', type:'Wartung', desc:'Planmäßige Schmierung aller beweglichen Teile', color:'#30D158' },
    { date:'03.04.2025', type:'Störung', desc:'Werkzeugwechsel-Sensor ausgefallen, ersetzt', color:'#FF9F0A' },
    { date:'14.02.2025', type:'Wartung', desc:'Hauptinspektion & Kalibrierung', color:'#30D158' },
  ],
  'M002': [
    { date:'02.05.2025', type:'Kritisch', desc:'Hydrauliköl-Leckage festgestellt, temporär behoben', color:'#FF453A' },
    { date:'28.04.2025', type:'Störung', desc:'Druckabfall unter Nennwert', color:'#FF9F0A' },
    { date:'15.03.2025', type:'Wartung', desc:'Ölwechsel & Dichtungsprüfung', color:'#30D158' },
  ],
  'M003': [
    { date:'10.05.2025', type:'Wartung', desc:'Software-Update & Kalibrierung', color:'#30D158' },
    { date:'20.03.2025', type:'Inspektion', desc:'Quartalskontrolle abgeschlossen', color:'#0A84FF' },
  ],
  'M004': [
    { date:'08.05.2025', type:'Kritisch', desc:'Druckventil defekt – Betrieb eingestellt', color:'#FF453A' },
    { date:'01.05.2025', type:'Störung', desc:'Abnormale Vibration und Überhitzung', color:'#FF453A' },
    { date:'10.02.2025', type:'Wartung', desc:'Letzte planmäßige Wartung', color:'#30D158' },
  ],
  'M005': [
    { date:'14.05.2025', type:'Wartung', desc:'Riemenspannung überprüft & Sensoren kalibriert', color:'#30D158' },
    { date:'01.04.2025', type:'Störung', desc:'Kurzer Bandstillstand wegen Materialstau', color:'#FF9F0A' },
  ],
  'M006': [
    { date:'15.02.2025', type:'Offline', desc:'Maschine für Umbau stillgelegt', color:'#48484A' },
    { date:'10.01.2025', type:'Kritisch', desc:'Spindelbruch – Totalausfall', color:'#FF453A' },
  ],
};

const KB_DATA = [
  { id:'KB001', tag:'MOTOR', title:'Motor überhitzt – Ursachenanalyse', excerpt:'Mögliche Ursachen: verstopfte Lüftungsschlitze, defektes Thermostat, überhöhte Last...', cat:'motor', detail:'Überhitzung von Motoren tritt häufig auf durch: 1) Verstopfte oder blockierte Lüftungsschlitze (Reinigung mit Druckluft), 2) Defektes Thermostat oder Kühlsystem, 3) Überhöhte mechanische Last, 4) Defekte Lager (ungewöhnliche Geräusche beachten). Sofortmaßnahme: Motor abstellen, Kühlung prüfen. Empfohlene Wartungsintervall: alle 500 Betriebsstunden.' },
  { id:'KB002', tag:'HYDRAULIK', title:'Hydrauliköl-Leckage identifizieren', excerpt:'Dichtungsschäden, Leitungsrisse oder gelockerte Verschraubungen sind häufigste Ursachen...', cat:'hydraulik', detail:'Leckagequellen systematisch eingrenzen: Sichtprüfung aller Anschlüsse und Leitungen. UV-Leckagemittel kann helfen, die Quelle zu finden. Typische Schwachstellen: Kolbenstangendichtungen, Rohrverschraubungen, Steuerventile. Hydrauliköl sofort auffangen (Umweltschutz). Nur zugelassene Dichtungswerkstoffe verwenden.' },
  { id:'KB003', tag:'ELEKTRIK', title:'Kabelschäden prüfen & beheben', excerpt:'Isolationsschäden durch mechanische Belastung, Wärme oder Alterung erkennen...', cat:'elektrik', detail:'Kabelprüfung: Sichtprüfung auf Abrieb, Risse, Verfärbungen. Isolationsmessung mit Megaohmmeter (Zielwert: >1MΩ). Häufige Fehlerquellen: Kabelschleppketten ohne ausreichenden Biegeradius, Scheuerstellen an Metallkanten, thermische Überlastung. Reparatur nur durch Elektrofachkraft.' },
  { id:'KB004', tag:'WARTUNG', title:'Wartungsintervalle – Checkliste', excerpt:'Tägliche, wöchentliche und monatliche Wartungsaufgaben für Industrieanlagen...', cat:'wartung', detail:'Täglich: Sichtprüfung, Ölstand, Druckluft prüfen. Wöchentlich: Reinigung, Schmierung kritischer Lager, Funktionstest aller Sicherheitseinrichtungen. Monatlich: Alle Verschleißteile prüfen, Messgeräte kalibrieren, Dokumentation aktualisieren. Jährlich: Hauptinspektion, alle Dichtungen tauschen, Elektrische Anlage prüfen lassen.' },
  { id:'KB005', tag:'MOTOR', title:'Vibrationsanalyse – Diagnose', excerpt:'Erhöhte Vibrationen können auf Unwucht, defekte Lager oder Resonanz hinweisen...', cat:'motor', detail:'Vibrationsmessungen in mm/s oder g. Grenzwerte nach ISO 10816: Klasse I <2.3 mm/s (gut). Ursachen: Unwucht des Rotors (statisch/dynamisch ausgleichen), Lagerschäden (Rauschpegel FFT-Analyse), Ausrichtungsfehler Kupplung, Resonanzerscheinungen im Fundament.' },
  { id:'KB006', tag:'HYDRAULIK', title:'Hydraulikdruck optimieren', excerpt:'Druckabfall, Druckspitzen und falsch eingestellte Sicherheitsventile beheben...', cat:'hydraulik', detail:'Druckeinstellung: Sicherheitsventil immer mindestens 10% über maximalem Betriebsdruck. Druckabfall: Leckage (intern/extern), verstopfte Filter, Pumpenabnutzung. Druckspitzen: Druckstoß-Dämpfer einbauen, Beschleunigungen reduzieren. Manometer regelmäßig kalibrieren.' },
  { id:'KB007', tag:'ELEKTRIK', title:'Frequenzumrichter – Fehlercodes', excerpt:'Die häufigsten FU-Fehlercodes und ihre Bedeutung im Industrieeinsatz...', cat:'elektrik', detail:'F001 – Überstrom: Last reduzieren, Rampenzeit erhöhen. F002 – Überspannung: Brems-Chopper prüfen. F003 – Unterspannung: Netzspannung prüfen. F004 – Übertemperatur: Kühlung prüfen, Umgebungstemperatur. F007 – Erdschluss: Kabeldurchgang zum Motor prüfen. Nach Reset: Ursache beheben, nicht nur zurücksetzen.' },
  { id:'KB008', tag:'WARTUNG', title:'Schmierplan – Industrielager', excerpt:'Korrekte Schmiermittelauswahl und Intervalle für Wälz- und Gleitlager...', cat:'wartung', detail:'Wälzlager: Lithiumbasisfett NLGI-2 als Standard. Nachschmiermenge: 0.005 × D × B (Gramm). Lagertemperatur als Indikator: +10-15°C über Umgebung normal, >+30°C Alarm. Gleitlager: Öl-Umlaufschmierung oder Druckumlaufschmierung je nach Drehzahl. Lagerspiel regelmäßig messen.' },
];

// ===================== TICKETS DATA =====================

function getTickets() {
  const def = [
    { id:'TK-0021', title:'Hydraulikleckage an Presse BP-200', machine:'M002', prio:'critical', status:'open', desc:'Ölfilm unter der Maschine entdeckt. Druckabfall beobachtet.', created: Date.now() - 86400000*2 },
    { id:'TK-0020', title:'Kompressor überhitzt', machine:'M004', prio:'critical', status:'in-progress', desc:'Betriebstemperatur über 90°C. Kühlsystem wird geprüft.', created: Date.now() - 86400000*3 },
    { id:'TK-0019', title:'Vibrationswarnung an CNC Alpha', machine:'M001', prio:'high', status:'open', desc:'Schwingungssensor hat Grenzwert überschritten.', created: Date.now() - 86400000*5 },
    { id:'TK-0018', title:'Wartung CNC Alpha fällig', machine:'M001', prio:'medium', status:'open', desc:'Planmäßige Inspektion laut Wartungsplan.', created: Date.now() - 86400000*7 },
    { id:'TK-0017', title:'Kabelprüfung Schweißroboter', machine:'M003', prio:'low', status:'done', desc:'Routineprüfung der Steuerleitung.', created: Date.now() - 86400000*10 },
    { id:'TK-0016', title:'Filteraustausch Kompressor', machine:'M004', prio:'high', status:'done', desc:'Luftfilter getauscht, Drucktest durchgeführt.', created: Date.now() - 86400000*14 },
  ];
  try {
    const s = localStorage.getItem('maintai_tickets');
    return s ? JSON.parse(s) : def;
  } catch(e) { return def; }
}

function saveTickets(t) {
  try { localStorage.setItem('maintai_tickets', JSON.stringify(t)); } catch(e) {}
}

let tickets = getTickets();
let currentFilter = 'all';

// ===================== NAVIGATION =====================

function navigate(page, el) {
  document.querySelectorAll('.page').forEach(p => p.classList.remove('active'));
  document.querySelectorAll('.nav-item').forEach(n => n.classList.remove('active'));
  document.getElementById('page-' + page).classList.add('active');
  el.classList.add('active');
  if(page==='tickets') renderTickets();
  if(page==='maschinen') renderMachines(MACHINES);
  if(page==='qr') renderQRButtons();
  window.scrollTo(0,0);
}

function showSection(id) {
  document.getElementById('qr-scanner').classList.add('hidden');
  document.getElementById('kb').classList.add('hidden');
  document.getElementById(id).classList.remove('hidden');
  if(id==='kb') renderKB(KB_DATA);
  if(id==='qr-scanner') renderQRButtons();
  window.scrollTo(0,0);
}

// ===================== TOAST =====================

function showToast(msg, type='blue') {
  const colors = { blue:'var(--accent)', green:'var(--accent2)', red:'var(--accent4)', orange:'var(--accent3)', gray:'var(--text2)' };
  const t = document.createElement('div');
  t.className = 'toast';
  t.innerHTML = `<span style="color:${colors[type]||colors.blue};font-size:14px">●</span> ${msg}`;
  document.getElementById('toast-container').appendChild(t);
  setTimeout(() => {
    t.classList.add('fade-out');
    setTimeout(() => t.remove(), 300);
  }, 3000);
}

// ===================== MODAL =====================

function closeModal(id) {
  const el = document.getElementById(id);
  el.classList.add('hidden');
}

// ===================== DASHBOARD =====================

function renderDashboard() {
  // Status strip
  const strip = document.getElementById('dashboard-strip');
  strip.innerHTML = MACHINES.map(m => {
    const sc = m.status==='online'?'#30D158':m.status==='warning'?'#FF9F0A':m.status==='critical'?'#FF453A':'#48484A';
    return `<div class="status-chip">
      <div class="dot" style="background:${sc}"></div>
      <div class="chip-name">${m.name.split(' ').slice(0,2).join(' ')}</div>
      <div class="chip-status">${m.status==='online'?'Online':m.status==='warning'?'Warnung':m.status==='critical'?'Kritisch':'Offline'}</div>
    </div>`;
  }).join('');

  // KPIs
  const t = getTickets();
  const online = MACHINES.filter(m=>m.status==='online').length;
  const openT = t.filter(tk=>tk.status==='open').length;
  const critT = t.filter(tk=>tk.prio==='critical').length;
  const done = t.filter(tk=>tk.status==='done').length;

  document.getElementById('dashboard-kpis').innerHTML = `
    <div class="kpi-card blue">
      <div class="kpi-icon blue"><svg viewBox="0 0 24 24" fill="none" stroke="var(--accent)" stroke-width="2" width="20" height="20"><rect x="2" y="7" width="20" height="14" rx="2"/><path d="M16 7V5a2 2 0 0 0-2-2h-4a2 2 0 0 0-2 2v2"/></svg></div>
      <div class="kpi-value">${online}/${MACHINES.length}</div>
      <div class="kpi-label">Maschinen Online</div>
      <div class="kpi-trend up">↑ Normalbetrieb</div>
    </div>
    <div class="kpi-card orange">
      <div class="kpi-icon orange"><svg viewBox="0 0 24 24" fill="none" stroke="var(--accent3)" stroke-width="2" width="20" height="20"><path d="M14 2H6a2 2 0 0 0-2 2v16a2 2 0 0 0 2 2h12a2 2 0 0 0 2-2V8z"/><line x1="12" y1="9" x2="12" y2="13"/><line x1="12" y1="17" x2="12.01" y2="17"/></svg></div>
      <div class="kpi-value">${openT}</div>
      <div class="kpi-label">Offene Störungen</div>
      <div class="kpi-trend ${openT>3?'down':'up'}">${openT>3?'↑ erhöht':'↓ normal'}</div>
    </div>
    <div class="kpi-card green">
      <div class="kpi-icon green"><svg viewBox="0 0 24 24" fill="none" stroke="var(--accent2)" stroke-width="2" width="20" height="20"><polyline points="22 12 18 12 15 21 9 3 6 12 2 12"/></svg></div>
      <div class="kpi-value">3</div>
      <div class="kpi-label">Wartungen heute</div>
      <div class="kpi-trend up">✓ Planmäßig</div>
    </div>
    <div class="kpi-card red">
      <div class="kpi-icon red"><svg viewBox="0 0 24 24" fill="none" stroke="var(--accent4)" stroke-width="2" width="20" height="20"><path d="M10.29 3.86L1.82 18a2 2 0 0 0 1.71 3h16.94a2 2 0 0 0 1.71-3L13.71 3.86a2 2 0 0 0-3.42 0z"/><line x1="12" y1="9" x2="12" y2="13"/><line x1="12" y1="17" x2="12.01" y2="17"/></svg></div>
      <div class="kpi-value">${critT}</div>
      <div class="kpi-label">Kritische Vorfälle</div>
      <div class="kpi-trend ${critT>1?'down':'up'}">${critT>1?'⚠ Aktion nötig':'✓ Unter Kontrolle'}</div>
    </div>
  `;

  // Chart
  const days = ['Mo','Di','Mi','Do','Fr','Sa','So'];
  const vals = [5,8,3,12,7,4,9];
  const max = Math.max(...vals);
  const total = vals.reduce((a,b)=>a+b,0);
  document.getElementById('total-incidents').textContent = total;
  document.getElementById('dashboard-chart').innerHTML = days.map((d,i) => {
    const h = Math.max(8, Math.round((vals[i]/max)*110));
    const isToday = i===4;
    return `<div class="chart-bar-wrap">
      <div class="chart-bar ${isToday?'accent':'red'}" style="height:${h}px;opacity:${isToday?1:0.55+i*0.05}"></div>
      <div class="chart-label">${d}</div>
    </div>`;
  }).join('');

  // Recent tickets
  const recent = getTickets().slice(0,3);
  document.getElementById('dashboard-tickets').innerHTML = recent.map(t => renderTicketCard(t)).join('') +
    `<button class="btn btn-secondary mt-8" onclick="navigate('tickets',document.querySelector('[data-page=tickets]'))">Alle Tickets anzeigen →</button>`;

  updateBadge();
}

// ===================== TICKETS =====================

function renderTicketCard(t, clickable=true) {
  const statusMap = { open:'orange', 'in-progress':'blue', done:'green' };
  const statusLabel = { open:'Offen', 'in-progress':'In Arbeit', done:'Erledigt' };
  const prioLabel = { low:'Niedrig', medium:'Mittel', high:'Hoch', critical:'Kritisch' };
  const m = MACHINES.find(m=>m.id===t.machine);
  const date = new Date(t.created).toLocaleDateString('de-DE',{day:'2-digit',month:'short',year:'numeric'});
  return `<div class="ticket-item" ${clickable?`onclick="openTicketDetail('${t.id}')"`:''}>
    <div class="ticket-prio ${t.prio}"></div>
    <div class="flex-between">
      <div class="ticket-id">${t.id}</div>
      <div class="badge ${statusMap[t.status]}">${statusLabel[t.status]}</div>
    </div>
    <div class="ticket-title">${t.title}</div>
    <div class="ticket-meta">
      <span class="ticket-machine">📍 ${m?m.name:'Unbekannte Maschine'}</span>
      <span class="badge ${t.prio==='critical'?'red':t.prio==='high'?'orange':t.prio==='medium'?'blue':'green'}">${prioLabel[t.prio]}</span>
      <span style="font-size:11px;color:var(--text3)">${date}</span>
    </div>
  </div>`;
}

function renderTickets() {
  const all = getTickets();
  const filtered = currentFilter==='all' ? all :
    currentFilter==='critical' ? all.filter(t=>t.prio==='critical') :
    all.filter(t=>t.status===currentFilter);
  const counts = {
    all: all.length,
    open: all.filter(t=>t.status==='open').length,
    'in-progress': all.filter(t=>t.status==='in-progress').length,
    done: all.filter(t=>t.status==='done').length,
    critical: all.filter(t=>t.prio==='critical').length,
  };
  document.getElementById('ticket-count-label').textContent = `${counts.open} offen · ${counts['in-progress']} in Arbeit · ${counts.done} erledigt`;

  if(filtered.length===0) {
    document.getElementById('tickets-list').innerHTML = `<div style="text-align:center;padding:40px 0;color:var(--text2)"><div style="font-size:40px;margin-bottom:12px">✅</div><div style="font-size:15px;font-weight:600">Keine Tickets gefunden</div></div>`;
    return;
  }
  document.getElementById('tickets-list').innerHTML = filtered.map(t => renderTicketCard(t)).join('');
  updateBadge();
}

function filterTickets(f, el) {
  currentFilter = f;
  document.querySelectorAll('#ticket-filters .ftab').forEach(e=>e.classList.remove('active'));
  el.classList.add('active');
  renderTickets();
}

function updateBadge() {
  const open = getTickets().filter(t=>t.status==='open').length;
  const b = document.getElementById('ticket-badge');
  b.textContent = open;
  b.style.display = open>0?'flex':'none';
}

function openTicketModal() {
  const sel = document.getElementById('t-machine');
  sel.innerHTML = MACHINES.map(m=>`<option value="${m.id}">${m.icon} ${m.name}</option>`).join('');
  document.getElementById('ticket-modal').classList.remove('hidden');
}

function createTicket() {
  const title = document.getElementById('t-title').value.trim();
  const machine = document.getElementById('t-machine').value;
  const prio = document.getElementById('t-prio').value;
  const desc = document.getElementById('t-desc').value.trim();
  if(!title) { showToast('⚠️ Bitte Titel eingeben','orange'); return; }
  const all = getTickets();
  const id = 'TK-' + String(all.length+22).padStart(4,'0');
  all.unshift({ id, title, machine, prio, status:'open', desc: desc||'Kein Kommentar', created: Date.now() });
  saveTickets(all);
  tickets = all;
  closeModal('ticket-modal');
  document.getElementById('t-title').value='';
  document.getElementById('t-desc').value='';
  showToast(`✓ Ticket ${id} erstellt`,'green');
  renderTickets();
  renderDashboard();
}

function openTicketDetail(id) {
  const t = getTickets().find(t=>t.id===id);
  if(!t) return;
  const m = MACHINES.find(m=>m.id===t.machine);
  const statusLabel = { open:'Offen', 'in-progress':'In Arbeit', done:'Erledigt' };
  const statusMap = { open:'orange', 'in-progress':'blue', done:'green' };
  const prioLabel = { low:'🟢 Niedrig', medium:'🔵 Mittel', high:'🟠 Hoch', critical:'🔴 Kritisch' };
  const date = new Date(t.created).toLocaleDateString('de-DE',{day:'2-digit',month:'long',year:'numeric',hour:'2-digit',minute:'2-digit'});
  document.getElementById('ticket-detail-content').innerHTML = `
    <div class="modal-handle"></div>
    <div class="modal-title">${t.id}</div>
    <div class="card card-sm" style="margin-bottom:16px;background:linear-gradient(135deg,rgba(10,132,255,0.08),transparent)">
      <div style="font-size:18px;font-weight:700;margin-bottom:8px">${t.title}</div>
      <div class="flex-between">
        <div class="badge ${statusMap[t.status]}">${statusLabel[t.status]}</div>
        <div style="font-size:13px;font-weight:600">${prioLabel[t.prio]}</div>
      </div>
    </div>
    <div style="display:grid;grid-template-columns:1fr 1fr;gap:10px;margin-bottom:16px">
      <div class="card card-sm"><div class="kpi-label">Maschine</div><div style="font-size:13px;font-weight:600;margin-top:4px">${m?m.name:'–'}</div></div>
      <div class="card card-sm"><div class="kpi-label">Erstellt am</div><div style="font-size:13px;font-weight:600;margin-top:4px">${date}</div></div>
    </div>
    <div class="input-group">
      <div class="input-label">Beschreibung</div>
      <div class="card card-sm" style="font-size:14px;line-height:1.6;color:var(--text2)">${t.desc}</div>
    </div>
    <div class="input-group">
      <div class="input-label">Status ändern</div>
      <div style="display:grid;grid-template-columns:1fr 1fr 1fr;gap:8px">
        <button class="btn btn-secondary btn-sm" onclick="changeStatus('${t.id}','open')">Offen</button>
        <button class="btn btn-secondary btn-sm" onclick="changeStatus('${t.id}','in-progress')">In Arbeit</button>
        <button class="btn btn-secondary btn-sm" onclick="changeStatus('${t.id}','done')">Erledigt</button>
      </div>
    </div>
    <button class="btn btn-danger" onclick="deleteTicket('${t.id}')">🗑 Ticket löschen</button>
  `;
  document.getElementById('ticket-detail-modal').classList.remove('hidden');
}

function changeStatus(id, status) {
  const all = getTickets();
  const t = all.find(t=>t.id===id);
  if(t) { t.status=status; saveTickets(all); }
  closeModal('ticket-detail-modal');
  const labels = { open:'Offen', 'in-progress':'In Arbeit', done:'Erledigt' };
  showToast(`✓ Status: ${labels[status]}`,'green');
  renderTickets();
  renderDashboard();
}

function deleteTicket(id) {
  const all = getTickets().filter(t=>t.id!==id);
  saveTickets(all);
  closeModal('ticket-detail-modal');
  showToast('🗑 Ticket gelöscht','orange');
  renderTickets();
  renderDashboard();
}

function previewAttach(e) {
  const f = e.target.files[0];
  if(!f) return;
  document.getElementById('t-attach-preview').classList.remove('hidden');
  document.getElementById('t-attach-name').textContent = f.name;
}

// ===================== MACHINES =====================

function renderMachines(list) {
  const statusMap = { online:'green', warning:'orange', critical:'red', offline:'gray' };
  const statusLabel = { online:'Online', warning:'Warnung', critical:'Kritisch', offline:'Offline' };
  document.getElementById('machines-list').innerHTML = list.map(m =>
    `<div class="machine-card" onclick="openMachineDetail('${m.id}')">
      <div class="machine-icon">${m.icon}</div>
      <div class="machine-info">
        <div class="machine-name">${m.name}</div>
        <div class="machine-loc">📍 ${m.loc}</div>
        <div class="machine-last">Letzte Wartung: ${m.lastMaint}</div>
      </div>
      <div class="badge ${statusMap[m.status]}">${statusLabel[m.status]}</div>
    </div>`
  ).join('') || `<div style="text-align:center;padding:40px 0;color:var(--text2)">Keine Maschinen gefunden</div>`;
}

function searchMachines(q) {
  const f = MACHINES.filter(m => m.name.toLowerCase().includes(q.toLowerCase()) || m.loc.toLowerCase().includes(q.toLowerCase()));
  renderMachines(f);
}

function openMachineDetail(id) {
  const m = MACHINES.find(m=>m.id===id);
  if(!m) return;
  const hist = MACHINE_HISTORY[id]||[];
  const statusMap = { online:'green', warning:'orange', critical:'red', offline:'gray' };
  const statusLabel = { online:'Online', warning:'Warnung', critical:'Kritisch', offline:'Offline' };
  document.getElementById('machine-modal-content').innerHTML = `
    <div class="modal-handle"></div>
    <div style="display:flex;align-items:center;gap:14px;margin-bottom:20px">
      <div style="width:52px;height:52px;border-radius:14px;background:var(--glass);border:1px solid var(--glass-border);display:flex;align-items:center;justify-content:center;font-size:26px">${m.icon}</div>
      <div>
        <div style="font-size:18px;font-weight:700">${m.name}</div>
        <div style="font-size:13px;color:var(--text2);margin-top:2px">📍 ${m.loc}</div>
      </div>
    </div>
    <div style="display:grid;grid-template-columns:1fr 1fr;gap:10px;margin-bottom:20px">
      <div class="card card-sm">
        <div class="kpi-label">Status</div>
        <div class="badge ${statusMap[m.status]}" style="margin-top:6px">${statusLabel[m.status]}</div>
      </div>
      <div class="card card-sm">
        <div class="kpi-label">Betriebsstunden</div>
        <div style="font-size:16px;font-weight:700;color:var(--accent);margin-top:4px">${m.runtime}</div>
      </div>
      <div class="card card-sm">
        <div class="kpi-label">Temperatur</div>
        <div style="font-size:16px;font-weight:700;color:${m.temp!=='–'&&parseInt(m.temp)>80?'var(--accent4)':'var(--accent2)'};margin-top:4px">${m.temp}</div>
      </div>
      <div class="card card-sm">
        <div class="kpi-label">Vibration</div>
        <div style="font-size:16px;font-weight:700;color:${m.vib!=='–'&&parseFloat(m.vib)>1.5?'var(--accent4)':'var(--accent2)'};margin-top:4px">${m.vib}</div>
      </div>
      <div class="card card-sm">
        <div class="kpi-label">Letzte Wartung</div>
        <div style="font-size:13px;font-weight:600;margin-top:4px">${m.lastMaint}</div>
      </div>
      <div class="card card-sm">
        <div class="kpi-label">Nächste Wartung</div>
        <div style="font-size:13px;font-weight:600;margin-top:4px;color:${m.nextMaint==='überfällig'?'var(--accent4)':'var(--text)'}">${m.nextMaint}</div>
      </div>
    </div>
    <div style="margin-bottom:20px">
      <div class="section-title" style="margin-bottom:12px">Fehlerverlauf (${hist.length} Einträge)</div>
      ${hist.map(h=>`<div class="hist-item">
        <div class="hist-dot" style="background:${h.color}"></div>
        <div>
          <div style="font-size:13px;font-weight:600">${h.type}: ${h.desc}</div>
          <div class="hist-date">${h.date}</div>
        </div>
      </div>`).join('')||'<div style="color:var(--text2);font-size:13px">Keine Einträge vorhanden.</div>'}
    </div>
    <button class="btn btn-primary" onclick="closeModal('machine-modal');navigate('tickets',document.querySelector('[data-page=tickets]'));setTimeout(openTicketModal,100)">+ Ticket für diese Maschine</button>
  `;
  document.getElementById('machine-modal').classList.remove('hidden');
}

// ===================== QR =====================

function renderQRButtons() {
  document.getElementById('qr-machine-btns').innerHTML = MACHINES.map(m =>
    `<div class="qr-machine-btn" onclick="openQRDetail('${m.id}')">
      <div class="icon">${m.icon}</div>
      <div class="name">${m.name.split(' ').slice(0,2).join(' ')}</div>
      <div class="id">${m.id}</div>
    </div>`
  ).join('');
}

function openQRDetail(id) {
  const m = MACHINES.find(m=>m.id===id);
  if(!m) return;
  const hist = MACHINE_HISTORY[id]||[];
  const statusMap = { online:'green', warning:'orange', critical:'red', offline:'gray' };
  const statusLabel = { online:'Online', warning:'Warnung', critical:'Kritisch', offline:'Offline' };
  document.getElementById('qr-detail-content').innerHTML = `
    <div class="modal-handle"></div>
    <div style="display:flex;align-items:center;gap:12px;margin-bottom:16px">
      <div style="font-size:32px">${m.icon}</div>
      <div>
        <div style="font-size:17px;font-weight:700">${m.name}</div>
        <div style="font-size:12px;color:var(--text2)">${m.id} · ${m.loc}</div>
      </div>
      <div class="badge ${statusMap[m.status]}" style="margin-left:auto">${statusLabel[m.status]}</div>
    </div>
    <div style="display:grid;grid-template-columns:1fr 1fr 1fr;gap:8px;margin-bottom:16px">
      <div class="card card-sm" style="text-align:center"><div class="kpi-label">Runtime</div><div style="font-size:14px;font-weight:700;color:var(--accent);margin-top:4px">${m.runtime}</div></div>
      <div class="card card-sm" style="text-align:center"><div class="kpi-label">Temp</div><div style="font-size:14px;font-weight:700;margin-top:4px">${m.temp}</div></div>
      <div class="card card-sm" style="text-align:center"><div class="kpi-label">Fehler</div><div style="font-size:14px;font-weight:700;color:var(--accent4);margin-top:4px">${m.errors}</div></div>
    </div>
    <div class="section-title mb-8">Letzte Ereignisse</div>
    ${hist.slice(0,3).map(h=>`<div class="hist-item"><div class="hist-dot" style="background:${h.color}"></div><div><div style="font-size:13px;font-weight:600">${h.type}: ${h.desc}</div><div class="hist-date">${h.date}</div></div></div>`).join('')}
    <button class="btn btn-primary" style="margin-top:16px" onclick="closeModal('qr-detail-modal');openTicketModal()">+ Störung melden</button>
  `;
  document.getElementById('qr-detail-modal').classList.remove('hidden');
  showToast(`📱 ${m.id} gescannt`,'blue');
}

// ===================== KB =====================

function renderKB(data) {
  document.getElementById('kb-list').innerHTML = data.map(k =>
    `<div class="kb-item" onclick="openKBDetail('${k.id}')">
      <div class="kb-tag">${k.tag}</div>
      <div class="kb-title">${k.title}</div>
      <div class="kb-excerpt">${k.excerpt}</div>
    </div>`
  ).join('');
}

function searchKB(q) {
  const f = KB_DATA.filter(k =>
    k.title.toLowerCase().includes(q.toLowerCase()) ||
    k.excerpt.toLowerCase().includes(q.toLowerCase()) ||
    k.tag.toLowerCase().includes(q.toLowerCase())
  );
  renderKB(f);
}

let kbFilter = 'all';
function filterKB(f, el) {
  kbFilter = f;
  document.querySelectorAll('#kb ~ .filter-tabs .ftab').forEach(e=>e.classList.remove('active'));
  // use direct approach
  el.classList.add('active');
  const q = document.getElementById('kb-search').value;
  let data = f==='all' ? KB_DATA : KB_DATA.filter(k=>k.cat===f);
  if(q) data = data.filter(k=>k.title.toLowerCase().includes(q.toLowerCase())||k.excerpt.toLowerCase().includes(q.toLowerCase()));
  renderKB(data);
}

function openKBDetail(id) {
  const k = KB_DATA.find(k=>k.id===id);
  if(!k) return;
  document.getElementById('kb-detail-content').innerHTML = `
    <div class="modal-handle"></div>
    <div class="kb-tag" style="font-size:11px">${k.tag}</div>
    <div class="modal-title" style="margin-top:8px">${k.title}</div>
    <div style="font-size:14px;line-height:1.7;color:var(--text2)">${k.detail}</div>
    <button class="btn btn-secondary" style="margin-top:20px" onclick="closeModal('kb-detail-modal')">Schließen</button>
  `;
  document.getElementById('kb-detail-modal').classList.remove('hidden');
}

// ===================== INIT =====================

document.addEventListener('DOMContentLoaded', () => {
  renderDashboard();
  updateBadge();

  // Ticket modal machine selector init
  const sel = document.getElementById('t-machine');
  if(sel) sel.innerHTML = MACHINES.map(m=>`<option value="${m.id}">${m.icon} ${m.name}</option>`).join('');
});
</script>
</body>
</html>
